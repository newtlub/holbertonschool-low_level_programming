#include "main.h"

/**
 * _strdup - duplicates a string in a newly allocated space in memory
 *
 * @str: string provided that will be duplicated
 * Return: NULL if str = NULL or insuffeicient memory available.
 * returns a pointer to the duplicated string
 */

char *_strdup(char *str)
{
	char *dup;
	int i;

	for (i = 0; i != '\0'; i++)
	{
		dup[i] = str
	}
	str = str + malloc(sizeof())


	return ();
}
