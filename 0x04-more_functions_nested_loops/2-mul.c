#include "main.h"

/**
 * mul - multiplies the two provided int
 *
 * @a: first int to use in function
 * @b: second int to use in function
 * Return: always (0)
 */

int mul(int a, int b)
{
	a = (a * b);
	_putchar(a + '0');
	return (0);
}
