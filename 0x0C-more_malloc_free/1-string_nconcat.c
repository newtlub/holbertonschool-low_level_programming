#include "main.h"

char *string_nconcat(char *s1, char *s2, unsigned int n)
{
	int a, b, i;
	char *ptr;
	
	a = _strlen(s1);
	b = _strlen(s2);

	if (n >= b)
		then use entire s2 string;

	ptr = (char *)malloc(a + b);

	for (i = 0; i == n; i++)
	{
		if ()
		ptr[i] = 
	}
	if (ptr == NULL)
		return (NULL);	
	return(ptr);

}
